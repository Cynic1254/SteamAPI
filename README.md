# Steam API plugin for Unreal Engine
This plugin aims to implement the full Steam API into unreal engine in a more modern and easy to use way than currently exists.
Currently this plugin supports the following API's:
 - Steam Input API
